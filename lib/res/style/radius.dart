import 'package:flutter/material.dart';

class Radius {
  static BorderRadius smallRadius = BorderRadius.circular(10);
  static BorderRadius mediumRadius = BorderRadius.circular(20);
  static BorderRadius largeRadius = BorderRadius.circular(30);
}
